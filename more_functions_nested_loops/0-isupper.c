#include "main.h"
#include <stdio.h>

/**
 * main - print a fonction shows upscales letter
 *
 * Return: Always 0.
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
		return (1);
	else
		return (0);
	return (0);
}
